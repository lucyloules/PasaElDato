@model IEnumerable<PasaElDato_v2.Models.imagen_producto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.path_imagenproducto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.path_imagenproducto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idimagenproducto }) |
            @Html.ActionLink("Details", "Details", new { id=item.idimagenproducto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idimagenproducto })
        </td>
    </tr>
}

</table>
