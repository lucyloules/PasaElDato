@model IEnumerable<PasaElDato_v2.Models.reserva>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.horas.dia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.producto.descripcion_producto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.usuario.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.horas.dia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.producto.descripcion_producto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.usuario.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idreserva }) |
            @Html.ActionLink("Details", "Details", new { id=item.idreserva }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idreserva })
        </td>
    </tr>
}

</table>
