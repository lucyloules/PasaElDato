@model IEnumerable<PasaElDato_v2.Models.servicio_habitacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descriocion_serviciohabitacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descriocion_serviciohabitacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idserviciohabitacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.idserviciohabitacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idserviciohabitacion })
        </td>
    </tr>
}

</table>
