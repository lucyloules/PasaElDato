@model IEnumerable<PasaElDato_v2.Models.region>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.region1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.region1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idregion }) |
            @Html.ActionLink("Details", "Details", new { id=item.idregion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idregion })
        </td>
    </tr>
}

</table>
