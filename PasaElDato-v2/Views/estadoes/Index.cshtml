@model IEnumerable<PasaElDato_v2.Models.estado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.estado1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.estado1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idestado }) |
            @Html.ActionLink("Details", "Details", new { id=item.idestado }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idestado })
        </td>
    </tr>
}

</table>
